package org.example;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        ResourceManager resourceManager = ResourceManager.getInstance();

        Resource resource = resourceManager.getResources();
        Resource resource1 = resourceManager.getResources();
        Resource resource2 = resourceManager.getResources();

        Resource resource3 = resourceManager.getResources();
        Resource resource4 = resourceManager.getResources();
        Resource resource5 = resourceManager.getResources();
    }
}